<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.xt</groupId>
	<artifactId>xiaoxingxing</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name/>
	<description/>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>21</java.version>
		<mybatis-plus.version>3.5.17</mybatis-plus.version>
		<!--
			Apache RocketMQ 官方维护的 Spring Boot Starter。
			这里不另外强制覆盖 rocketmq-client-java 版本，让 Starter 使用它发布时验证过的客户端组合。
		-->
		<rocketmq-spring.version>2.3.6</rocketmq-spring.version>
		<!-- XXL-JOB 3.4.2 的官方构建基线与本项目一致：Spring Boot 4.1 / JDK 17+。 -->
		<xxl-job.version>3.4.2</xxl-job.version>
		<!-- Flowable 8.0.0：BPMN 流程与 DMN 决策表学习模块。 -->
		<flowable.version>8.0.0</flowable.version>
		<!-- Drools 10.2 使用 JDK 21 作为推荐运行环境。 -->
		<drools.version>10.2.0</drools.version>
	</properties>
	<dependencies>
		<!--
			XXL-JOB 官方只提供 core，并未提供 Spring Boot 自动配置 Starter。
			本项目显式创建唯一的 XxlJobSpringExecutor Bean，便于学习其注册、回调与停机生命周期。
		-->
		<dependency>
			<groupId>com.xuxueli</groupId>
			<artifactId>xxl-job-core</artifactId>
			<version>${xxl-job.version}</version>
		</dependency>
		<!-- Flowable BPMN 流程引擎自动配置与 Spring 集成。 -->
		<dependency>
			<groupId>org.flowable</groupId>
			<artifactId>flowable-spring-boot-starter-process</artifactId>
			<version>${flowable.version}</version>
		</dependency>
		<!-- Flowable DMN 决策表引擎，与请假流程的路由决策配合使用。 -->
		<dependency>
			<groupId>org.flowable</groupId>
			<artifactId>flowable-spring-boot-starter-dmn</artifactId>
			<version>${flowable.version}</version>
		</dependency>
		<!-- 8.0.0 自动配置中直接使用的 ProcessEngineConfigurator 实现（starter 未传递可选依赖）。 -->
		<dependency>
			<groupId>org.flowable</groupId>
			<artifactId>flowable-spring-configurator</artifactId>
			<version>${flowable.version}</version>
		</dependency>
		<!-- 8.0.0 自动配置中直接使用的 DMN configurator 实现（starter 未传递可选依赖）。 -->
		<dependency>
			<groupId>org.flowable</groupId>
			<artifactId>flowable-dmn-spring-configurator</artifactId>
			<version>${flowable.version}</version>
		</dependency>
		<!-- Flyway 数据库迁移学习模块：由 Spring Boot 在启动时执行版本化 SQL。 -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-flyway</artifactId>
		</dependency>
		<!-- Flyway 12 将 PostgreSQL 支持拆为独立数据库扩展。 -->
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<!--
			RocketMQ 5.x 可靠消息学习模块：
			1. Starter 自动创建 RocketMQClientTemplate，生产者无需手工管理客户端生命周期；
			2. @RocketMQMessageListener 创建 PushConsumer，消费成功/失败通过 ConsumeResult 返回；
			3. 5.x gRPC 客户端连接 Proxy，而不是直接把 NameServer 地址当作 endpoints。

			注意：Starter 2.3.6 官方发布基线早于 Spring Boot 4.1。本项目先保留官方版本组合，
			实际运行兼容性需要在本地启动后验证，不能仅凭依赖能够解析就下结论。
		-->
		<dependency>
			<groupId>org.apache.rocketmq</groupId>
			<artifactId>rocketmq-v5-client-spring-boot-starter</artifactId>
			<version>${rocketmq-spring.version}</version>
		</dependency>
		<!-- Drools 订单规则学习模块：规则文件从 classpath 加载到内嵌 KIE 容器。 -->
		<dependency>
			<groupId>org.drools</groupId>
			<artifactId>drools-engine</artifactId>
			<version>${drools.version}</version>
		</dependency>
		<!-- Drools 10 单独提供 kmodule.xml 解析支持，用它注册 orderRuleBase 和 orderRuleSession。 -->
		<dependency>
			<groupId>org.drools</groupId>
			<artifactId>drools-xml-support</artifactId>
			<version>${drools.version}</version>
		</dependency>
		<!-- 当前应用在运行时直接编译 classpath 中的 DRL，需要 Drools 的 MVEL 约束实现。 -->
		<dependency>
			<groupId>org.drools</groupId>
			<artifactId>drools-mvel</artifactId>
			<version>${drools.version}</version>
		</dependency>
		<!-- MongoDB 动态问卷学习模块：同步 Repository + MongoTemplate。 -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-mongodb</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-elasticsearch</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>3.0.3</version>
		</dependency>
		<dependency>
			<groupId>org.redisson</groupId>
			<artifactId>redisson</artifactId>
			<version>3.35.0</version>
		</dependency>
		<dependency>
			<groupId>com.baomidou</groupId>
			<artifactId>mybatis-plus-spring-boot4-starter</artifactId>
			<version>${mybatis-plus.version}</version>
		</dependency>
		<dependency>
			<groupId>com.baomidou</groupId>
			<artifactId>mybatis-plus-jsqlparser</artifactId>
			<version>${mybatis-plus.version}</version>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<!--
				JSONB TypeHandler 在主代码中直接使用 PostgreSQL JDBC 的 PGobject，
				因此驱动必须进入编译 classpath，不能再声明为 runtime-only。
				默认 compile 作用域同时覆盖编译和运行，不影响应用连接 PostgreSQL。
			-->
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
    </dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
					<execution>
						<id>default-testCompile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>testCompile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>mac-osx-aarch64</id>
			<activation>
				<os>
					<family>mac</family>
					<arch>aarch64</arch>
				</os>
			</activation>
			<dependencies>
				<dependency>
					<groupId>io.netty</groupId>
					<artifactId>netty-resolver-dns-native-macos</artifactId>
					<classifier>osx-aarch_64</classifier>
					<scope>runtime</scope>
				</dependency>
			</dependencies>
		</profile>

		<profile>
			<id>mac-osx-x86_64</id>
			<activation>
				<os>
					<family>mac</family>
					<arch>x86_64</arch>
				</os>
			</activation>
			<dependencies>
				<dependency>
					<groupId>io.netty</groupId>
					<artifactId>netty-resolver-dns-native-macos</artifactId>
					<classifier>osx-x86_64</classifier>
					<scope>runtime</scope>
				</dependency>
			</dependencies>
		</profile>
	</profiles>

</project>
