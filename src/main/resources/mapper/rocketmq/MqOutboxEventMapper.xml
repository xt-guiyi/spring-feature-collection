<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xt.xiaoxingxing.playground.rocketmq.mapper.MqOutboxEventMapper">

    <sql id="outboxColumns">
        id,
        aggregate_type AS aggregateType,
        aggregate_id AS aggregateId,
        event_type AS eventType,
        schema_version AS schemaVersion,
        topic_name AS topicName,
        message_tag AS messageTag,
        message_key AS messageKey,
        message_group AS messageGroup,
        deliver_at AS deliverAt,
        payload::text AS payload,
        status,
        retry_count AS retryCount,
        next_retry_at AS nextRetryAt,
        locked_at AS lockedAt,
        last_error AS lastError,
        created_at AS createdAt,
        published_at AS publishedAt
    </sql>

    <!--
        订单事实与 Outbox 必须在同一 PostgreSQL 事务内插入。本 SQL 只创建 PENDING 行而不直接发消息，
        避免“订单已提交、进程却在事务中途崩溃导致消息丢失”的双写窗口。
        payload 已由后端序列化为合法 JSON，显式 CAST 为 JSONB 让数据库拒绝损坏的文本。
    -->
    <insert id="insert" parameterType="com.xt.xiaoxingxing.playground.rocketmq.entity.MqOutboxEvent">
        INSERT INTO mq_outbox_event (
            id, aggregate_type, aggregate_id, event_type, schema_version,
            topic_name, message_tag, message_key, message_group, deliver_at,
            payload, status, retry_count, next_retry_at, created_at
        ) VALUES (
            #{id}, #{aggregateType}, #{aggregateId}, #{eventType}, #{schemaVersion},
            #{topicName}, #{messageTag}, #{messageKey}, #{messageGroup}, #{deliverAt},
            CAST(#{payload} AS JSONB), 'PENDING', 0,
            COALESCE(#{nextRetryAt}, CURRENT_TIMESTAMP), COALESCE(#{createdAt}, CURRENT_TIMESTAMP)
        )
    </insert>

    <!--
        原子领取必须是一条 SQL：
        1. FOR UPDATE SKIP LOCKED 让多个调度实例跳过已被其他事务锁住的行，而不是彼此阻塞；
        2. UPDATE ... RETURNING 在同一语句把候选行改为 PROCESSING 并交回完整消息，避免“先查后改”重复领取；
        3. 过期的 PROCESSING 租约可被重新领取，覆盖发送成功但进程尚未来得及回写状态就崩溃的窗口。
        此机制提供至少一次发布，不承诺端到端恰好一次，消费者仍必须使用唯一约束幂等。
    -->
    <select id="claimPublishable"
            resultType="com.xt.xiaoxingxing.playground.rocketmq.entity.MqOutboxEvent"
            affectData="true"
            flushCache="true">
        WITH candidates AS (
            SELECT id
            FROM mq_outbox_event
            WHERE (
                status IN ('PENDING', 'FAILED')
                AND next_retry_at &lt;= CURRENT_TIMESTAMP
            ) OR (
                status = 'PROCESSING'
                AND locked_at &lt; #{lockExpiredBefore}
            )
            ORDER BY created_at ASC, id ASC
            LIMIT #{batchSize}
            FOR UPDATE SKIP LOCKED
        )
        UPDATE mq_outbox_event target
        SET status = 'PROCESSING',
            locked_at = CURRENT_TIMESTAMP,
            last_error = NULL
        FROM candidates
        WHERE target.id = candidates.id
        RETURNING
            target.id,
            target.aggregate_type AS aggregateType,
            target.aggregate_id AS aggregateId,
            target.event_type AS eventType,
            target.schema_version AS schemaVersion,
            target.topic_name AS topicName,
            target.message_tag AS messageTag,
            target.message_key AS messageKey,
            target.message_group AS messageGroup,
            target.deliver_at AS deliverAt,
            target.payload::text AS payload,
            target.status,
            target.retry_count AS retryCount,
            target.next_retry_at AS nextRetryAt,
            target.locked_at AS lockedAt,
            target.last_error AS lastError,
            target.created_at AS createdAt,
            target.published_at AS publishedAt
    </select>

    <!--
        locked_at 是本次领取返回给 worker 的租约令牌。若租约过期后另一 worker 已重新领取同一行，
        locked_at 会被刷新；旧 worker 即使迟到收到发送结果，也必须因为时间不匹配而影响 0 行，
        不能覆盖新 worker 正在处理的状态。受影响 1 行才代表当前租约持有者成功结束发布。
    -->
    <update id="markPublished">
        UPDATE mq_outbox_event
        SET status = 'PUBLISHED',
            published_at = CURRENT_TIMESTAMP,
            locked_at = NULL,
            last_error = NULL
        WHERE id = #{id}
          AND status = 'PROCESSING'
          AND locked_at = #{claimedLockedAt}
    </update>

    <!--
        retry_count 在数据库内递增，避免多个线程各自读到旧计数后互相覆盖。
        达到上限的行转为 DEAD，需要人工修复后另行创建/重放，不再被调度器自动领取。
        locked_at 同样参与条件更新：租约已过期并被重新领取时，旧 worker 的迟到失败回写必须影响 0 行，
        否则它会错误增加重试次数并把新 worker 的 PROCESSING 改成 FAILED/DEAD。
    -->
    <update id="markFailed">
        UPDATE mq_outbox_event
        SET retry_count = retry_count + 1,
            status = CASE
                WHEN retry_count + 1 &gt;= #{maxPublishRetries} THEN 'DEAD'
                ELSE 'FAILED'
            END,
            next_retry_at = #{nextRetryAt},
            locked_at = NULL,
            last_error = #{lastError}
        WHERE id = #{id}
          AND status = 'PROCESSING'
          AND locked_at = #{claimedLockedAt}
    </update>

    <select id="selectById" resultType="com.xt.xiaoxingxing.playground.rocketmq.entity.MqOutboxEvent">
        SELECT <include refid="outboxColumns"/>
        FROM mq_outbox_event
        WHERE id = #{id}
    </select>

    <select id="selectPage" resultType="com.xt.xiaoxingxing.playground.rocketmq.entity.MqOutboxEvent">
        SELECT <include refid="outboxColumns"/>
        FROM mq_outbox_event
        <where>
            <if test="status != null and status != ''">status = #{status}</if>
        </where>
        ORDER BY created_at DESC, id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(*)
        FROM mq_outbox_event
        <where>
            <if test="status != null and status != ''">status = #{status}</if>
        </where>
    </select>

</mapper>
