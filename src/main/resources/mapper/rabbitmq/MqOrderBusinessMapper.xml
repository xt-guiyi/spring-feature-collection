<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xt.xiaoxingxing.playground.rabbitmq.mapper.MqOrderBusinessMapper">

    <select id="selectOrderById" resultType="com.xt.xiaoxingxing.playground.postgresql.entity.PgOrder">
        SELECT id,
               user_id AS userId,
               order_no AS orderNo,
               total_amount AS totalAmount,
               status,
               created_at AS createdAt
        FROM orders
        WHERE id = #{orderId}
    </select>

    <!-- 状态条件和更新处于同一条 SQL，支付与超时取消并发时只能有一方影响一行。 -->
    <update id="markPaid">
        UPDATE orders
        SET status = 'PAID'
        WHERE id = #{orderId}
          AND status = 'PENDING'
    </update>

    <update id="markCancelled">
        UPDATE orders
        SET status = 'CANCELLED'
        WHERE id = #{orderId}
          AND status = 'PENDING'
    </update>

    <!-- 固定 product_id 升序恢复库存，降低多个事务以相反顺序获取产品行锁形成死锁的概率。 -->
    <select id="selectOrderProducts"
            resultType="com.xt.xiaoxingxing.playground.postgresql.entity.PgOrderProduct">
        SELECT id,
               order_id AS orderId,
               product_id AS productId,
               quantity,
               unit_price AS unitPrice
        FROM order_products
        WHERE order_id = #{orderId}
        ORDER BY product_id ASC
    </select>

    <update id="restoreStock">
        UPDATE products
        SET stock = stock + #{quantity}
        WHERE id = #{productId}
    </update>

</mapper>
